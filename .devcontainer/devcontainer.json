{
  "name": "ReviseIQ Android",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "remoteUser": "vscode",
  "customizations": {
    "vscode": {
      "extensions": [
        "vscjava.vscode-java-pack",
        "fwcd.kotlin"
      ],
      "settings": {
        "java.configuration.updateBuildConfiguration": "automatic"
      }
    }
  },
  "postCreateCommand": "if [ -n \"$GEMINI_API_KEY\" ]; then echo \"GEMINI_API_KEY=$GEMINI_API_KEY\" > .env; fi && ./gradlew --version && ./gradlew :app:assembleDebug",
  "mounts": [
    "source=reviseiq-gradle-cache,target=/home/vscode/.gradle,type=volume"
  ]
}
